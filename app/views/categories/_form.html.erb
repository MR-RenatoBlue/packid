<%= form_with(model: category, local: true) do |form| %>
  <%= render "shared/form_errors", form: form %>
  
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
